# Docker Environment Configuration
# Copy this file to .env when running with Docker

APP_NAME="Meracikopi"
APP_ENV=production
APP_DEBUG=false
APP_URL=http://localhost:8000

# Database Configuration (matches docker-compose.yml)
DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=meracikopi
DB_USERNAME=meracikopi
DB_PASSWORD=secret
DB_ROOT_PASSWORD=rootsecret

# Cache & Session (using Redis in Docker)
CACHE_STORE=redis
SESSION_DRIVER=redis
QUEUE_CONNECTION=redis

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=null

# Midtrans Configuration
# Replace with your actual keys
MIDTRANS_SERVER_KEY=Mid-server-hlY0t9zaV14-e8pwBJl4-slU
MIDTRANS_CLIENT_KEY=Mid-client-Nyi43V4R5rdNM9BD
MIDTRANS_IS_PRODUCTION=false

# Service Fee
SERVICE_FEE=0

# Mail Configuration (use mailhog for testing)
MAIL_MAILER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@meracikopi.com"
MAIL_FROM_NAME="${APP_NAME}"

# Logging
LOG_CHANNEL=stack
LOG_LEVEL=info
